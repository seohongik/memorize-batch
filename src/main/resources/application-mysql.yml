spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/bat?characterEncoding=UTF-8&serverTimezone=UTC&rewriteBatchedStatements=true
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: hongik
      password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    jdbc:
      initialize-schema: never